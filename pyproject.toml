[project]
name = "sokany"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "telethon",
    "gspread",
    "google-auth",
    "python-dotenv",
    "httpx",
]
